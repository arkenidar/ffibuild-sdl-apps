{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug LOVE2D (found in PATH)",
            "type": "lua-local", // tomblind.local-lua-debugger-vscode
            "request": "launch",
            "program": {
                "command": "luajit"
            },
            "args": [
                "main.lua"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}