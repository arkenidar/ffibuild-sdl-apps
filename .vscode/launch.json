{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "app.lua",
            "type": "lua-local", // tomblind.local-lua-debugger-vscode
            "request": "launch",
            "program": {
                "command": "luajit"
            },
            "args": [
                "app.lua"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}